import { StyleSheet } from "react-native";

// Add styles for the extracted text and adjust as necessary
export const styles = StyleSheet.create({
  container: {
    justifyContent: "center",
    alignItems: "center",
    padding: 10,
  },
  image: {
    width: 300,
    height: 300,
    marginBottom: 20,
  },
  extractedText: {
    textAlign: "center",
  },
});
